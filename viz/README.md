# Visualization Utilities for Wordle Solver

`viz` is a Python package that provides visualization utilities for the Wordle solver implemented in Rust. It includes functions to visualize the information gain of different guesses and to analyze the distribution of word probabilities.

## Development

To create the virtual environment and install dependencies, run:

```bash
uv venv
```
