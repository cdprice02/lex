[project]
name = "viz"
version = "0.1.0"
description = "visualization utilities for wordle solver"
requires-python = ">=3.13"
readme = "README.md"
dependencies = ["matplotlib>=3.10.9", "pandas>=3.0.3"]

[dependency-groups]
dev = ["ipykernel>=7.2.0"]
